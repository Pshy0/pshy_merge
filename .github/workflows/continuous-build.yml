name: "Continuous Build"

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  continuous-build:
    name: "Continuous Build"
    runs-on: "ubuntu-latest"

    steps:
    - uses: actions/checkout@v1
    - name: "Combine"
      run: Make
      
